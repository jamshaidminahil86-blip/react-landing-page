function Header() {
  return (
    <header>
      <h2>ReactStart</h2>

      <nav>
        <a href="#home">Home</a>
        <a href="#features">Features</a>
        <a href="#about">About</a>
      </nav>
    </header>
  );
}

export default Header;